import React from "react";
import styled from "styled-components";

const CardContainer = styled.section`
  padding: 1.5em;
`;

const Card = () => (
  <CardContainer>
    <p>test from card</p>
  </CardContainer>
);

export default Card;
